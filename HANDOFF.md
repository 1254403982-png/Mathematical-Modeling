# HANDOFF：新成员（人 or Agent）从这里开始

2026 高教社杯 C 题「微网与外部电网电力调控策略」团队协作仓库。
远端：`https://github.com/1254403982-png/Mathematical-Modeling`

## 0. 先搞清"你该看哪条分支"

| 分支 | 谁看 | 里面有什么 | 分支里**没有**什么 |
|---|---|---|---|
| **`main`** | **论文手** | `docs/`（15 份模型文档）、`paper/`（写作说明 + 结果锚点）、`BRANCHES.md` | 任何代码 |
| **`modeler`** | 模型手 | `main` + Q1 参照解 + 模板标签修正工具 | 正式实现 |
| **`programmer`** | **编程手** | `main` + `src/q1..q4/` 任务卡 + 实现 | 由编程手逐步写入 |
| `feat/q1`…`feat/q4` | 编程手 | 单问工作分支，从 `programmer` 分出 | — |

**归属规则（硬）**：`docs/**` 只有模型手能改，其他人只读；`src/qN/**` 只有编程手能改。
合并方向 `feat/qN → programmer → main`。规则全文见 `BRANCHES.md`。

**数据与模板不在仓库里**（竞赛材料）。附件放本地 `data/` 下，或用命令行参数指定路径。

---

## 1. 给编程手 Agent 的开场话术（整段复制）

> 你是 2026 高教社杯 C 题「微网与外部电网电力调控策略」的编程手。
> 仓库：`https://github.com/1254403982-png/Mathematical-Modeling`
>
> **第一步，按顺序读这 5 个文件：**
> 1. `git checkout programmer`（这个分支才是你的）
> 2. `BRANCHES.md` —— 协作规则、你能改哪些目录
> 3. `docs/AGENTS.md` —— 时间映射、已定案口径、验收规则。**有冲突一律以它为准**
> 4. `src/q1/README.md` —— 你的第一个任务卡
> 5. `docs/编程手开工包_Q1.md` —— Q1 的权威规格（公式、约束、算法伪代码都在里面）
>
> **工作方式**
> - `docs/**` 只读，不要改（口径由模型手维护）；你的代码写在 `src/q1/`，Q2 用 `src/q2/`，以此类推。
> - 顺序 `Q1 → Q2 → Q3 → Q4`。**做完 Q1 先停下来汇报，不要连着做。**
> - Q1 是全链条标尺（时间映射、单位换算、约束边界都在它上面验证），Q1 数字对上了后面才不会白跑。
> - 每问交付：可复现代码 + 输出文件 + 验收指标（各残差、锚点实测值、求解状态与耗时）。
>
> **Q1 要做三件事**
> ① 主模型 = LP（`scipy.optimize.linprog(method="highs")`，去掉 0-1 变量）；
> ② 另写连续状态 DP（凸分段线性 + 斜率合并，伪代码在开工包 §3.2），**只用于验证，不参与出结果**；
> ③ LP 与 DP **独立**求解并互证。
>
> **验收锚点（对不上就是 bug）**
> - 最优购电费 `35126.948589` 元
> - 无储能基准 `48052.046591` 元
> - 表1 六时段 `0 / 480.41 / 0 / 445.43 / 531.89 / 0` kWh
> - LP 与 DP 的费用、购电量、24:00 储电量三项差 `< 1e-6`
>
> **铁律（违反即返工）**
> - 时间：模型时段 $t=$ 数组下标 $+1$；槽 $t$ 覆盖 $[(t-1)\times10,\ t\times10)$ 分钟，标签取**右端点**。
> - **按位置回填输出模板，绝不按标签文字对齐**——官方模板的 144 个 10 分钟标签整体偏了 10 分钟。
> - 全电量制：附件功率 $\times\frac16$ 转 kWh；计费时不再重复乘步长。
> - **因果隔离**：计划与场景阶段不得读取当天实际负荷、光伏；场景只用第 $d$ 天之前的数据。
> - 求解器只用 scipy/HiGHS。不用商业求解器，不用遗传算法 / 粒子群等启发式。
> - 结果另存 `outputs/`，**不覆盖**官方模板。
>
> **环境**：`pip install -r requirements.txt`（numpy / openpyxl / scipy）。附件放 `data/`，或用 `--input <路径>` 指定。
>
> **口径有疑问**：先查 `docs/AGENTS.md` 的决策台账；仍不确定就回来问，**不要自行假设后当作题目条件**。

---

## 2. 给论文手 Agent 的开场话术（整段复制）

> 你是 2026 高教社杯 C 题「微网与外部电网电力调控策略」的论文手。
> 仓库：`https://github.com/1254403982-png/Mathematical-Modeling`
>
> **第一步，按顺序读这 5 个文件：**
> 1. `git checkout main` —— **必须切到 `main`**，这里才是给你看的版本（其它分支是代码）
> 2. `docs/00_文档索引.md` —— 全部材料的索引与用途
> 3. `paper/README.md` —— 写作说明、图表建议、三条必须写进论文的说明
> 4. `docs/论文框架_依据范文重建.md` —— 篇章结构（已按范文重建）
> 5. `paper/结果锚点.md` —— 四问数字
>
> **材料怎么用**
> - 建模章节底稿 = `docs/编程手开工包_Q1..Q4.md` 的模型部分 + `docs/Q2_模型搭建思路.md`。
> - 多问共用的框架（连续状态 DP 价值函数、两阶段随机规划、因果执行器）见 `docs/AGENTS.md` 的决策台账。
> - 数据章节的加分点：`docs/附件数据结构核对.md`（时间轴、模板标签偏移、附件3 相位偏差）——值得单独写一小节。
> - 文献清单唯一来源：`docs/Q2-Q4_相关论文与建模逻辑链.md` §2（8 篇，带 DOI）。
>
> **⚠️ 数字纪律（最重要）**
> - `paper/结果锚点.md` 里，**只有 Q1 是 ✅已复现**（`35126.948589` 等），可以直接用；
> - **Q2 / Q3 / Q4 标 ⏳待复现有的是外部参考值**，编程手复现前**不要写进论文**；
> - **不要引用任何第三方材料的数字**。已发现一份外部材料把"无储能基准费用"算成 39990 元、节费率 12%，
>   是错的——正确是 **48052.05 元 / 26.9%**。论文里别抄那个 12%。
>
> **三条必须主动写进论文的说明**
> 1. 官方模板 144 个 10 分钟标签整体右移了 10 分钟，我们按位置回填；
> 2. 因果隔离——计划与场景阶段不使用当天实际数据（这是"可实施"与"后见之明"的分界）；
> 3. 能力边界——计划阶段不对冲紧急购电的 5 倍罚金，改进方向是两阶段随机规划。
>    主动写出来，比被评委问出来好。
>
> **不要做**：不要自己编数字；缺的等编程手出结果；不要改 `docs/**`（那是模型手的口径文件，只读）。

---

## 3. 最短版（只想说一句话时）

- **编程手**：`git checkout programmer`，读 `BRANCHES.md` 和 `src/q1/README.md` 开工，**Q1 做完先回报**。
- **论文手**：`git checkout main`，从 `docs/00_文档索引.md` 进，数字看 `paper/结果锚点.md`（⏳ 的别用）。

---

## 4. 常用命令

```bash
# 首次
git clone https://github.com/1254403982-png/Mathematical-Modeling.git
cd Mathematical-Modeling

# 编程手：开工
git checkout programmer && git pull
git checkout -b feat/q1        # 从 programmer 分出单问分支
python -m pip install -r requirements.txt
python src/q1/run.py --input "data/附件1.xlsx"

# 论文手：阅读
git checkout main && git pull

# 模型手：更新口径文档
git checkout modeler
# ... 改 docs/ ...
git commit -am "docs: ..." && git push origin modeler
git checkout main && git merge modeler && git push origin main
```
