# 编程手交接（模型手 → 编程 Agent）

> 本文件是模型手交给编程 Agent 的**交接说明**，用于快速上手，不替代规格文档。
> 工作目录：`D:\数模\`。

---

## 一、开场话术（可直接粘贴给编程 Agent）

> 你是 2026 高教社杯 C 题（微网与外部电网电力调控策略）的**编程手**。工作目录固定 `D:\数模\`。
>
> **先读 `D:\数模\AGENTS.md`（唯一入口 + 决策台账），再读对应问题的 `编程手开工包_Q*.md`，然后动手。**
>
> **环境**：用受管 Python `C:\Users\杨曼曼\.workbuddy\binaries\python\envs\default\Scripts\python.exe`（已装 scipy，含 HiGHS）；**没有 pulp，不要装环境**。
>
> **顺序**：Q1 → Q2 → Q3 → Q4。**先做 Q1，做完把验收结果报给我再往下。**
>
> **Q1 做三件事**：① 主模型 = LP（`scipy.optimize.linprog(method="highs")`，去 0-1 变量）→ 用它求解并填 `result1.xlsx`；② **另写**连续状态 DP（凸分段线性 + 斜率合并，伪代码见开工包 §3.2）；③ LP 与 DP **独立**求解并互证。
>
> **口径有疑问**：先查 AGENTS.md 决策台账；仍不确定就回来问我，**不要自行假设后当作题目条件**。

---

## 二、关键口径（最易写错，务必核对）

| # | 口径 | 易错点 |
|---|---|---|
| 1 | **时间索引 $t=i+1$**，附件标签是区间**右端点** | 会写成 $t=i$ |
| 2 | **按位置回填，严禁按标签字符串平移** | 官方模板 10 分钟列标签整体偏 10 分钟 |
| 3 | 全电量制：附件功率 **×1/6** 转 kWh，计费不再重复乘步长 | 漏乘 / 重复乘 |
| 4 | **因果隔离**：计划与场景阶段**不得读当天实际** $L,V$；场景只用第 $d$ 天之前数据 | 偷读当天数据，破坏对照 |
| 5 | Q1 **主模型是 LP**（去 $z_t$）；DP 只作互证 | 自作主张上 MILP 或跳过 DP |
| 6 | 结果**另存 `输出/`**，不覆盖官方模板；**不用启发式算法** | 覆盖模板 / 用遗传算法 |
| 7 | **终值系数 $v=$ 已知凌晨（0:00–5:00）电价均值 $\div\ \eta_d$**（参考 0.481548） | 写成乘 $\eta_d$，或硬编码 0.33417 / 0.3713 |
| 8 | 结算一律 **B 口径**（按部分定价）：$\sum c\min(g,a)+1.5\sum c\delta^++0.5\sum c\delta^-$ | 写回 A 口径（照付不议），导致全年调整量恒为 0 |

**官方模板位置（位置回填）**：宽表第 $k$ 个数据位（Excel 列 $k+1$）↔ 模型 $t=k$；result1 计划购电量行 $r$ ↔ $t=r-1$；表1 六时段 → result1 第 **62 / 74 / 86 / 98 / 110 / 122** 行。

---

## 三、验收锚点（对不上就是 bug）

| 问 | 锚点 |
|---|---|
| **Q1** | 最优购电费 **35126.948589 元**；无储能基准 $\sum p\max(0,L-V)$ = **48052.046591 元**；节费 26.8981%；表1 = `0 / 480.41 / 0 / 445.43 / 531.89 / 0`；DP 与 LP 差 <1e-6 |
| **Q2** | 量级参考（完整稿）**13,566,395.37** 元 = 计划 12,782,973.47 + 紧急 783,421.90；**不设硬阈值**。硬判据：紧急费恰为 0（说明读了当天实际值）或总量差一个数量级 |
| **Q3** | 量级参考（完整稿）**13,024,699.25** 元（较 Q2 省 541,696.11）；**不设硬阈值**。硬判据同上，另加：全年 $\sum\lvert a-g\rvert$ 应 >0（若恒为 0 说明结算口径写回了 A 口径） |
| **Q4** | 4-2 **14,269,748.14**；4-3 **13,716,889.66**；价格预知参照 14,181,755.45 / 13,623,595.92；离线**下界 12,780,883.29** |

> 锚点是"有无量级错误"的判据，**不是要凑出来的目标**；若程序结果与锚点差一个数量级，先查单位（×1/6）与时间索引。

---

## 四、文件清单

### Q1 必需（5 个）
`C题.pdf`、`附件1.xlsx`、`result1.xlsx`、`编程手开工包_Q1.md`、`AGENTS.md`

### Q2 必需（6 个）
`C题.pdf`、`附件1.xlsx`（电价）、`附件2.xlsx`（负荷+光伏）、`result2.xlsx`、`编程手开工包_Q2.md`、`AGENTS.md`

> Q2 禁止读取附件3。预测采用"**同类日最近 3 日距离加权 + 30 日样本外偏差修正**"（负荷与光伏同口径）。开工包已展开全部公式，不需要另找带公式编号的外部附件。

### 辅助（强烈建议一并给）
- `附件数据结构核对.md`：时间轴、**模板标签偏移**、表1/表2 映射——最容易踩的坑都在这；
- `结果模板_标签修正说明.md` + `模板_标签修正/`：位置回填的对照（Q1/Q2 都用）；
- Q1 额外：`Q1_参照解与核对.py`（外部锚点脚本，可直接跑）；
- Q2 额外：`Q2_模型搭建思路.md`——**已同步为现行口径**，供论文手写作参考；编程规格仍只有 `编程手开工包_Q2.md` 一份。

### 不要给
`历史版本/`、`建模参考思路_对比.md`（第三方材料与旧口径，会干扰），以及 Q3/Q4 的文件（做到再给）。

---

## 五、铁律（违反即返工）

1. 时间索引 $t=i+1$；标签是区间右端点；**只用位置回填**。
2. 功率 ×1/6 → kWh；计费不重复乘步长。
3. 因果隔离：计划/场景不读当天实际；场景只用 $d$ 之前数据。
4. 确定性 LP（HiGHS），不用遗传算法/粒子群等启发式。
5. 结果文件复制官方模板另存 `输出/`，不覆盖原模板。

---

## 六、交付与回报

每个问题交付：**可复现代码 + 填好的 xlsx + 验收指标**（各残差、锚点实测值、求解状态与耗时；Q1 另报 DP 与 LP 的差异与 DP 段数）。

**Q1 做完先回报模型手**，确认数字对得上再进 Q2——Q1 是全链条的标尺（单位/时间/约束都在这验证）。
