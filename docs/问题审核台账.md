# 问题审核台账

> 目的：把"文档之间、文档与题面之间、公式与文字之间"的**自相矛盾**逐条揪出来、留痕、修正。
> 一条矛盾如果只靠"读一遍"是抓不住的，必须按下面的维度逐类扫描。**每轮审核都在本表追加，不覆盖历史。**

建立日期：2026-09-12

---

## 一、审核维度（每轮按这八类扫）

| # | 类别 | 典型形态 | 怎么扫 |
|---|---|---|---|
| **D1** | **公式方向/量纲矛盾** | 同一个量在不同文件里一个乘 $\eta$、一个除 $\eta$；单位一个 kWh 一个 kW | 对同一符号全局 grep，逐处比方向与量纲 |
| **D2** | **决策与正文矛盾** | 决策台账已改为 X，正文仍写 Y（旧口径残留） | 台账每条决策 → 反查所有引用处 |
| **D3** | **数值锚点过期** | 旧口径算出的数字仍被当"标尺"用 | grep 关键数字，确认其口径与天数 |
| **D4** | **符号未定义** | 公式里出现的量在全文找不到定义（如 $\hat N_{d\mid\tau}$） | 逐个符号反查定义 |
| **D5** | **信息集/因果矛盾** | 计划阶段引用了当天实际数据；某子问越级使用了不该用的附件 | 检查每个模块的输入清单 |
| **D6** | **编号与题面不符** | 表1/表2/表3 的所指与题面不一致 | 与 `C题.pdf` 逐表对照 |
| **D7** | **可实施性缺陷** | 方法依赖不存在的数据（如"去年同期"，而附件2 只有 2025 一年） | 对每个数据需求问"这份数据真的存在吗" |
| **D8** | **验收优先级** | 顺序颠倒：先上高级算法，却还没把单位/SOC/费用算对、没堵住信息泄露 | 每轮先问三条：① 单位/SOC/费用算对了吗（Q1 逐位复现是标尺）② 有未来信息泄露吗 ③ 统一储能模型把"日前计划—实时修正—紧急购电—动态电价"串起来了吗 |

---

## 二、已发现问题与处理

### 本轮（2026-09-12，Q4 审核）

| # | 类别 | 文件 · 位置 | 问题 | 判定依据 | 处理 | 级别 |
|---|---|---|---|---|---|---|
| ~~R-01~~ | D1 | `编程手开工包_Q4.md` §3 式(50) | 判"$1/\eta$ 与 $\eta_d$ 方向相反，**必有一错**" | ~~$E$ 是储电量：期末多存 1 kWh → 明日可交付 $\eta_d$ kWh → 省 $\eta_d c$~~ | **本条撤回（2026-09-12 夜）**。完整稿 $v=\bar c_{0:00\text{–}5:00}/\eta$ 经实测**精确复现**（$0.433393/0.9=0.481548$）。凌晨电池处在**充电**边际上，多存 1 kWh 相当于次日少充 $1/\eta$ kWh 交流电，省 $c/\eta$；由 SOC 递推的 KKT 关系，充电绑定时储能影子价 $\lambda=\pi/\eta$、放电绑定时 $\lambda=\eta\pi$，凌晨窗口取前者。**"必有一错"的判断本身是错的**——这不是矛盾，是两种边际渠道的建模选择，而正解就是 $1/\eta$。v3 的"乘 $\eta_d$"已改回 | ⚫ **已撤回** |
| R-02 | D2 | `AGENTS.md` §已统一的建模口径 | 写 `q_t ∈ [0, R_t]`，与决策 #11（$U_t\ge0$ 无上界）矛盾 | 决策 #11 已定 | **已改**为 $g_t+R_t+d_t=L_t+c_t+U_t$ | 🟠 P2 表述 |
| R-03 | D2 | `AGENTS.md` §已统一的建模口径 | 写"充放电互斥：使用二元变量"，与决策 #9（全 LP）矛盾 | 决策 #9 已定 | **已改**为"不设 0-1 变量，求解后断言 $C_tD_t\le10^{-8}$" | 🟠 P2 |
| R-04 | D2 | `AGENTS.md` §已统一的建模口径 | 写"求解器：`scipy.optimize.milp`，144 binary"，与决策 #9 矛盾 | 同上 | **已改**为 `linprog(method="highs")` | 🟠 P2 |
| R-05 | D2 | `论文框架_依据范文重建.md` §3.2 | 仍用旧记法"弃光变量 $q_t\in[0,R_t]$" | 决策 #11 | **已改**为 $U_t\ge0$，并加注说明改名 | 🟠 P2 |
| R-06 | D3/留痕 | `附件数据结构核对.md` §8.4 | 记"我方是 MILP（$z_t$ 互斥）"——是 2026-09-11 当时的真实状态 | 留痕不篡改 | **已加后注**："现已改全 LP，本行留痕不改" | 🟡 P3 |
| R-07 | D3/留痕 | `C题_微网电力调控_要点整理.md`（2 处） | 记"144 个 0-1 变量的 MILP" | 同上 | **已加后注** | 🟡 P3 |
| ~~R-08~~ | D3 | `编程手开工包_Q4.md` §2 | 判"改善 41% 是 oracle 上界，可行值 32%" | ~~据"3 日均净负荷预测得 MAE 0.0996"推断~~ | **本条撤回（2026-09-12 夜）**。完整稿表 21 的 0.050347 是**相对 AR(1) 0.085833** 改善 41.3%；其 $\hat N$ 明确定义为"当日已观测净负荷电量 + 剩余预测电量"，且对历史日重建当时可得的预测。**41% 是可实现值**。我上轮用"3 日等权净负荷"得到 0.0996，差异根源是**负荷预测器质量**（完整稿分解预测负载 MAPE 3.191%，远优于 3 日等权），不是 oracle。已改回 | ⚫ **已撤回** |
| R-09 | D5 | `编程手开工包_Q4.md` 对照设计 | 4-2 用净负荷回归、4-3 用 AR(1)，两模型精度不同 → "4-3 vs 4-2"里混入了模型差异 | 完整稿 §6.3 已自陈，并给出同频对照（4-2 同频 AR 对照省 7103.80 元；B 口径下 4-3 改用净负荷回归/AR-X 反而增费 1412.87 / 1379.52 元），且明示"**不按实现后的逐日费用选模**" | **降级**：主方案改为采用完整稿配置（4-2 净负荷回归 / 4-3 AR(1)）；**2×2 全交叉保留为敏感性层**（必做、四格全报），用于分离"策略价值"与"模型价值"。原文"P0 污染"的定性过重，已修正 | 🟠 P2 结论 |
| R-10 | D5 | `编程手开工包_Q4.md` | 4-2 是否使用附件3 未定案 | 题面问题2 清单无附件3；问题4 清单是并集；模板同构 | **已定案**：4-2 不用、4-3 用（决策 #13） | 🔴 P0 |
| R-11 | D4 | `编程手开工包_Q4.md` §2 式(47) | $\hat N_{d\mid\tau}$ 全文无定义 | 完整稿 §6.1 有明确定义 | **已写死并修正量纲**：= 当日已观测净负荷**电量（kWh）** + 剩余时段的净负荷预测电量，整体除以 $10^5$ 缩放；其负荷预测器 = Q2 的"日电量×日内形状"分解。v3 写的"同类日距离加权+30日修正"与"日均净负荷（kW）"**均作废** | 🔴 P0 |

### 同一轮追加（2026-09-12 夜，通读完整稿后）

| # | 类别 | 文件 · 位置 | 问题 | 判定依据 | 处理 | 级别 |
|---|---|---|---|---|---|---|
| R-16 | D2 | `AGENTS.md` 决策 #2/#5；`编程手开工包_Q2.md` §2；`_Q3.md` §3；`_Q4.md` §2.3 | 预测口径写"最近 3 个同类日**距离加权** + **30 日滚动偏差修正**"，却声称"与完整稿同源" | 完整稿 §4.1 式(17a–17c)：形状取同类型最近 3 日**等权**平均曲线归一化；日电量用**类型切换对数比例中位数 $\beta_d$** 递推；**没有**30 日偏差修正——30 条残差只用于构造**场景**。原文还明确反对"同星期均值把缓慢变化的日电量水平滞后数周" | **已改**：AGENTS 新增"预测口径"小节（含全部公式）；Q2 §2 重写为分解法；Q3 §3 重写；Q4 §2.3 改为指向 Q2 分解式 | 🔴 P0 |
| R-17 | D1 | `AGENTS.md` 决策 #1；`编程手开工包_Q2.md` §4.2；`_Q3.md` §6 | 终值 $v=\eta_d p_{\min}=0.33417$ | 完整稿 $v=\bar c_{0:00\text{–}5:00}/\eta_d=0.481548$，实测精确复现 | **已改**为"以凌晨电价均值为基准、除以 $\eta_d$"；旧值差 44% 且方向相反 | 🔴 P0 |
| R-18 | D1 | 全项目命名 | "口径 A" 两处含义冲突：Q2 缺口度量 A vs 结算口径 A | 完整稿 §5.3/§5.6：**A = 照付不议（调减受支配）、B = 按部分定价** | **已统一**：结算采用完整稿 A/B；Q2 缺口一律写"缺口相对 $g^{plan}$"（决策 #17） | 🟠 P1 |
| R-19 | D3 | `AGENTS.md` §已统一口径；`Q1_参照解与核对.py` | Q1 声明"两阶段字典序（第二阶段 $\min\sum(c+d)$）"，完整稿是**单目标 LP** | 实测：单目标 LP 得 $\Sigma c=20740.666131$（与完整稿逐位一致）；两阶段 lexmin 得 20740.2288。因 $E_T=E_0$ 时恒有 $\Sigma d=0.81\Sigma c$，第二阶段等价于最小化 $\Sigma c$，会改变解 | **已改**：Q1 = 单目标 LP（决策 #18）；参照解脚本待改（见 T-10） | 🔴 P1 |
| R-20 | D6 | `AGENTS.md` §数据与时间对齐 | 写"表3/表4 的紧急购电时间段" | 题面 表3 = 指定日期紧急购电结果；无"表4" | **已改**为"题目表3" | 🟡 P3 |
| R-21 | D1 | `编程手开工包_Q4.md` §2.2/§2.6 | 价格下限只写了一个 | 完整稿 §6.1：预测**水平**下限 0.01 元/kWh，$[\cdot]_\varepsilon$ 偏差修正下限 $10^{-4}$ 元/kWh——**两个不同的下限** | **已分写** | 🟡 P3 |
| R-22 | D2 | `编程手开工包_Q4.md` §3.2 | 4-2 只写"沿用 Q2 信息集" | 完整稿 §6.3：4-2 在 6/12/18 **仍更新价格情景与储能价值**，只是保持 $a=g$、不重订采购 | **已补** | 🟠 P2 |

### Q2 编程手结果验收（2026-09-12 上午，读 result2_final.xlsx + q2.py + verify 脚本）

| # | 类别 | 位置 | 问题 | 判定依据 | 处理 | 级别 |
|---|---|---|---|---|---|---|
| R-23 | D2 | 编程手 `src/q2/q2.py` `forecast_one_day` | 预测器用 **"同类日距离加权 + 30 日偏差修正"**（v5），不是本轮定案的**"日电量×日内形状分解"**（v6） | grep 见 `inverse_distance_selection` + `BIAS_WINDOW_DAYS=30` + `load_bias`；verify 脚本 method=`main_weighted_plus_30day_bias` | **待定 + 实测（2026-09-12 上午）**：334 天实测 v5 与 v6 负荷 MAPE **打平（3.10% vs 3.11%）**，都比同星期均值 5.06% 好约 40%。**"改 v6 更准"不成立**；改 v6 的唯一收益是"与完整稿/论文叙事一致 + 机理清晰（类型切换倍率）"。v5 的"30日偏差修正"本身也有效（即 bias correction），R-16 曾判其"概念错"**过重**。是否让编程手重跑由用户拍板 | 🟠 P1 |
| R-24 | D6 | 编程手 `src/q2/q2.py` 写表（1607/1616 行） | 时间映射用 **循环移位**：`displayed = current_plan[1:] + [次日 t1]`，即第 2 列装 $t=2$、第 145 列装次日 $t=1$ | **（原"必须纠正"判定已撤销，2026-09-12 用户定案）** Q2 采用"**文字意义 = 物理时间**"口径：官方模板第 2 列标签 `0:10-0:20` 即物理 0:10-0:20（$t=2$）、末列 `0:00+1-0:10+1` 即次日 0:00-0:10（次日 $t=1$），按文字对齐**正是**循环移位。**编程手当前写法与新口径一致，无需改** | ✅ 按新口径正确。连带：① 表1 六时段取值列 = **61/73/85/97/109/121**（非位置回填的 62/74/86/98/110/122）；② 当天 0:00-0:10 记在前一天末列 `0:00+1`（跨日借用，官方模板文字自带）；③ 与 Q1"按位置回填"口径差一格，Q1 是否统一待用户确认 | 🟢 已定 |

**Q2 复现结果**（读 `src/output2/result2_final.xlsx`，与完整稿锚点对照）：

| 项 | 编程手 | 锚点 | 偏差 |
|---|---:|---:|---:|
| 计划费 | 12,726,501.86 | 12,782,973.47 | −0.44% |
| 计划量 | 20,606,005.8 kWh | 20,652,373.9 | −0.22% |
| 紧急量 | 150,669.7 kWh | 143,647.2 | +4.89% |
| 紧急费 | 778,922.03 | 783,421.90 | −0.57% |
| **总费用** | **13,505,423.89** | **13,566,395.37** | **−0.45%** |

结论：**框架、约束、因果、跨日连续、场景审计 全部自检通过**（verify 脚本 12 节），总费用 −0.45% 属**复现成功**。偏差主要来自 R-23（预测器口径差），紧急量 +4.89% 但紧急费 −0.57%（多买的紧急电落在低价时段，DP 保留水平实现细节差异）。

### 更早发现（补录）

| # | 类别 | 位置 | 问题 | 处理 |
|---|---|---|---|---|
| R-12 | D7 | 两份外部材料 | 都用"**去年同期**若干日"生成场景；但附件2 仅 2025 一年，**无去年同期**，不可实施 | 我方方案不使用该特征；论文中若引用外部方法需注明 |
| R-13 | D6 | `编程手开工包_Q1.md` / `附件数据结构核对.md` | "表1"一度被误当作"6 个 4 小时块的购电总量" | 已更正：**表1 = 六个指定 10 分钟时段**（$t=61/73/85/97/109/121$ ↔ `result1` 第 62/74/86/98/110/122 行）；4 小时块属表2 |
| R-14 | D1 | `编程手开工包_Q1.md` §1.2 | 残留**循环移位**指令（`g[1:]+g[:1]`），与"按位置回填"定案冲突 | 已删除；循环移位提案已否决归档 |
| R-15 | D3 | `AGENTS.md` 早期版本 | 必读清单只列到 Q1；未记录 $t=i+1$ 定案 | 已重写为唯一入口 + 唯一决策台账 |

---

## 三、待审清单（下一轮做）

| # | 类别 | 待查内容 | 为什么值得查 |
|---|---|---|---|
| T-01 | D4 | Q2 / Q3 开工包中是否还有**未定义符号**（逐个符号反查定义） | R-11 就是这么抓出来的；Q4 有，Q2/Q3 也可能有 |
| T-02 | D1 | $B_t$（储能未利用量）在 Q2 §4 与 Q3 中的定义是否一致；是否与 $U_t$ 重复计一次 | 两个"未利用量"并存容易重复计 |
| T-03 | D6 | 全文的"表1/表2/表3/表4"编号是否与 `C题.pdf` 一致（题面 表3 = 指定日期紧急购电、表4 = 填写示例） | 文档中多处写"表3/表4 的紧急购电时间段" |
| ~~T-04~~ | D1 | $v$ 的数值与方向 | ✅ **已闭环（R-16/R-17）**：旧值 0.33417 是"乘 $\eta$ + 用全天最低价"，正解是"除 $\eta$ + 用凌晨均值"。已核对全项目无第二处方向错误 |
| T-05 | D6 | `result1/2/3/4-2/4-3` 五个模板的**列数与表头**是否与题面逐一对应（含"全天购电量/购电费"列） | 回填前必须锁死 |
| T-06 | D3 | `结果模板_标签修正说明.md` 与 `模板_标签修正/` 生成脚本是否仍一致 | 模板改过一次，防止再漂 |
| T-07 | D5 | 全四问的**因果隔离**审查：每个计划阶段函数的输入签名 | 最容易被偷偷加入当天实际值 |
| T-08 | D2 | `C题_模型规格说明书_模型手.md` 是否还有未更新的 ☐ 待定项 | 它是模型规格的正式文档 |
| T-09 | D4 | **符号 $R_t$ 被两用**：早期文档用 $R_t$ 表示光伏电量，Q3 开工包用 $R_t$ 表示 DP 执行器的动态保留水平 | 论文符号表必须唯一；已在本轮 `论文正文素材_按章节.md` 中统一为 光伏 $=V_{d,t}$、保留水平 $=R_t$，但**历史文档中的 $R_t$ 需逐处确认** |
| ~~T-10~~ | D3 | `Q1_参照解与核对.py` 两阶段字典序 vs Q1 单目标 LP | ✅ **已闭环**：脚本已重写为单目标 LP，实测逐位复现 35126.948589 / 59482.698998 / 20740.666131 / 16799.939566 |
| ~~T-11~~ | D2 | 论文正文素材 / Q4 审核 / 交接 / Q2 思路 的预测口径·终值·命名同步 | ✅ **已闭环**：四份均已同步到分解预测 + 除η + B 结算 |
| ~~T-12~~ | D3 | `_上传包/` 与仓库 `docs/` 按本轮重建 | ✅ **已闭环**：`_上传包` 重建（19 份）、仓库 docs 刷新、`docs/docs` 嵌套已删。**推送仍待用户终端执行** |
| T-13 | D8 | **验收优先级（用户定下的常驻纪律）**：先把单位/SOC/费用算对 → 再处理预测误差；先保证无未来信息泄露 → 再谈算法高级；用统一储能模型把"日前计划—实时修正—紧急购电—动态电价"串起来 | 每轮自查/夜班自动核查**先过这三条**，再谈 DP/随机规划的加分项。当前缺口：Q2–Q4 全年费用仍未由我方代码复现（只有完整稿参考值） |

---

## 四、审核与 git 留痕的对应

每次修正**必须**产生一条 git 提交，提交信息包含审核编号，便于论文写作时追溯。

**本轮的留痕形态是"更正 + 撤回"两级**：

```
审核 R-16~R-22: 通读完整稿后修正预测口径 / 终值方向 / Q1 口径

【撤回】
- R-01 撤回：v = c̄(0:00-5:00)/η 经实测精确复现（0.433393/0.9 = 0.481548）。
  原判"除 η 与乘 η 必有一错"错误——那不是矛盾，是两种边际渠道的建模选择。
  v3 的"乘 η_d"已改回除 η。
- R-08 撤回：41% 是相对 AR(1) 的可实现改善，不是 oracle 上界。
  原判据（用 3 日等权净负荷得 MAE 0.0996）的差异根源在负荷预测器质量。

【更正】
- R-16 预测口径 → 完整稿式(17a–17c) 日电量×日内形状分解 + 类型切换倍率 β_d
  （删除 v5 的"同类日距离加权 + 30 日滚动偏差修正"）
- R-17 终值 v → c̄(0:00–5:00)/η_d = 0.481548
- R-18 命名 → 结算 A=照付不议 / B=按部分定价；Q2 缺口改称"相对 g^plan"
- R-19 Q1 → 单目标 LP（两阶段 lexmin 会改变 Σc）
- R-20/21/22 表号、两个价格下限、4-2 日内仍更新价值
```

**为什么撤回也要留痕**：审核本身会出错。把"判过什么、后来为什么推翻"完整写下来，
论文里可以据此写一节"模型自洽性检查"，读者（评委）看到的是**检查过程**，不只是结论。
**禁止**为了让台账好看而删掉被撤回的条目——这正是"不覆盖历史结论"规则的意义。

**论文写作时**：§「模型的分析和检验」可写三条**真实发生过**的检验：
① **量纲/方向检查**——终值系数的 $\eta$ 方向曾两度改动，最终以 SOC 递推的 KKT 关系
（充电绑定 $\lambda=\pi/\eta$、放电绑定 $\lambda=\eta\pi$）与数值复现（0.481548）定案；
② **口径可归因性**——4-2/4-3 的价格模型不同，用 2×2 交叉表把"策略价值"与"模型价值"分离；
③ **一致性检查**——本文档各口径与完整稿逐条对照（见 §二）。
另外 R-16（**预测器的实现细节能把结论从 −32% 翻到 +21%**）本身就是很好的敏感性分析素材。
